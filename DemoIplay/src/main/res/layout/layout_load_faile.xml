<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_load_faile"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:visibility="visible" >

    <ImageView
        android:id="@+id/img_load_faile"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/img_load_failed" />

    <TextView
        android:id="@+id/tv_loading_wrong"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:text="网络异常请重试"
        android:textColor="@color/gray"
        android:textSize="20sp" />

    <Button
        android:id="@+id/btn_refresh"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:background="@drawable/selector_btn_refresh"
        android:minHeight="40dp"
        android:minWidth="120dp"
        android:text="点击刷新"
        android:textColor="@color/gray"
        android:textSize="20sp" />

</LinearLayout>